---
HOSTS:
  centos-5-x86:
    platform: el-5-x86_64
    hypervisor : docker
    image: centos:5
    docker_image_commands:
      - 'yum install -y locales rsync wget tar'
CONFIG:
  type: foss
  log_level: debug
